# Copy to .env and fill in your values. Never commit .env to git.

# VectorBT Pro API token — get from https://vectorbt.pro/account/
# Used by `make install-vbt` to install from the private package index.
VBT_TOKEN=your_token_here

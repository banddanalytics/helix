---
phase: 03-alpha-engines
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - src/alpha/signal_types.py
  - tests/alpha/__init__.py
  - tests/alpha/conftest.py
  - tests/alpha/test_regime_detector.py
  - tests/alpha/test_calibration.py
  - tests/alpha/test_cointegration.py
  - tests/alpha/test_carry.py
  - tests/alpha/test_features.py
  - tests/alpha/test_walk_forward.py
  - tests/alpha/test_ensemble.py
  - tests/alpha/test_orchestrator.py
autonomous: true
requirements:
  - ALPH-01
  - ALPH-02
  - ALPH-03
  - ALPH-04
  - ALPH-05
  - ALPH-06
  - ALPH-07
  - ALPH-08
  - ALPH-09

must_haves:
  truths:
    - "shap 0.51.0 is installed and importable in the project venv"
    - "All 9 test files exist under tests/alpha/ with meaningful test stubs"
    - "Signal schema types are defined and importable by all engines"
    - "pytest discovers tests/alpha/ and all stubs pass (xfail or skip)"
  artifacts:
    - path: "tests/alpha/conftest.py"
      provides: "Shared fixtures for alpha engine tests"
      min_lines: 20
    - path: "src/alpha/signal_types.py"
      provides: "SignalRow dataclass and SIGNAL_COLUMNS constant"
      exports: ["SignalRow", "SIGNAL_COLUMNS", "RegimeState"]
    - path: "pyproject.toml"
      provides: "shap in mypy overrides"
      contains: "shap.*"
  key_links:
    - from: "tests/alpha/conftest.py"
      to: "src/alpha/signal_types.py"
      via: "import for fixtures"
      pattern: "from src.alpha.signal_types import"
---

<objective>
Install shap, create the signal schema types contract, and scaffold all test files for Phase 3 alpha engines.

Purpose: Every subsequent plan depends on the test infrastructure and signal type definitions. Without this Wave 0 setup, no engine can write tests or produce correctly-typed signals.
Output: Working test scaffold with 9 test files, shared conftest.py, signal_types.py with the signal schema, and shap installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-alpha-engines/03-CONTEXT.md
@.planning/phases/03-alpha-engines/03-RESEARCH.md
@_docs/Phase_3_Alpha_Engines.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/data/pit_manager.py:
```python
def pit_read(
    library: str,
    symbol: str,
    as_of_timestamp: pd.Timestamp,
    *,
    store_uri: str = "lmdb://./arctic_data",
    columns: list[str] | None = None,
    snapshot: str | None = None,
) -> pd.DataFrame:
```

From src/execution/swap_rates.py:
```python
@dataclass(frozen=True)
class CarryResult:
    carry_long: float
    carry_short: float
    net_carry: float

class SwapRateCalculator:
    @staticmethod
    def compute_annualized_carry(
        swap_long: float, swap_short: float, point: float, mid_price: float,
    ) -> CarryResult:
```

From src/data/arctic_store.py:
```python
def get_store(uri: str = "lmdb://./arctic_data") -> adb.Arctic:
def get_library(name: str, *, uri: str = "lmdb://./arctic_data") -> adb.Library:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shap, add mypy overrides, create signal types contract</name>
  <files>pyproject.toml, src/alpha/signal_types.py</files>
  <read_first>
    - pyproject.toml (current mypy overrides, dependencies)
    - src/alpha/__init__.py (existing stub)
    - src/execution/swap_rates.py (CarryResult for reference)
    - _docs/Phase_3_Alpha_Engines.md (signal schema from D-01)
    - .planning/phases/03-alpha-engines/03-CONTEXT.md (D-01, D-02, D-03 signal decisions)
  </read_first>
  <action>
    1. Install shap: `.venv/bin/pip install shap==0.51.0`
    2. Add `"shap.*"` to the existing `[[tool.mypy.overrides]]` module list in pyproject.toml (the list starting at line 22 with `"MetaTrader5.*"`, `"hmmlearn.*"`, etc.)
    3. Create `src/alpha/signal_types.py` with:
       - `import enum` and `from dataclasses import dataclass`
       - `class RegimeState(enum.IntEnum)`: TRENDING=0, MEAN_REVERTING=1, CRISIS=2
       - `@dataclass` class `SignalRow` with fields per D-01:
         - `symbol: str`
         - `engine: str`
         - `direction: int` (int8: +1/0/-1)
         - `strength: float` (float32 [0,1])
         - `regime: int` (int8, current regime state)
         - `z_score: float | None = None` (cointegration)
         - `ml_prob: float | None = None` (ML)
         - `carry_rank: float | None = None` (carry)
       - `SIGNAL_COLUMNS: list[str]` constant listing all column names in order
       - `REGIME_ACTIVATION: dict[RegimeState, list[str]]` mapping per D-05:
         - `RegimeState.TRENDING: ["ml_engine", "carry_engine"]`
         - `RegimeState.MEAN_REVERTING: ["cointegration_engine"]`
         - `RegimeState.CRISIS: []`
       - `CONFIGURED_PAIRS: list[tuple[str, str]]` = `[("AUDUSD", "NZDUSD"), ("EURUSD", "GBPUSD"), ("USDJPY", "USDCHF")]`
       - `CROSS_ASSET_SYMBOLS: list[str]` = `["EURUSD", "GBPUSD", "AUDUSD", "NZDUSD", "USDJPY", "USDCHF"]`
    4. Update `src/alpha/__init__.py` to export `RegimeState`, `SignalRow`, `SIGNAL_COLUMNS` from signal_types
  </action>
  <verify>
    <automated>.venv/bin/python -c "import shap; print(shap.__version__)" && grep -c "shap" pyproject.toml && .venv/bin/python -c "from src.alpha.signal_types import SignalRow, RegimeState, SIGNAL_COLUMNS, REGIME_ACTIVATION, CONFIGURED_PAIRS; print(f'OK: {len(SIGNAL_COLUMNS)} columns, {len(RegimeState)} states')"</automated>
  </verify>
  <acceptance_criteria>
    - `shap.__version__` prints `0.51.0`
    - `pyproject.toml` contains `"shap.*"` in the mypy overrides module list
    - `SignalRow` has fields: symbol, engine, direction, strength, regime, z_score, ml_prob, carry_rank
    - `RegimeState` has values: TRENDING=0, MEAN_REVERTING=1, CRISIS=2
    - `REGIME_ACTIVATION[RegimeState.CRISIS]` is `[]`
    - `CONFIGURED_PAIRS` has 3 tuples
    - `CROSS_ASSET_SYMBOLS` has 6 strings
  </acceptance_criteria>
  <done>shap installed, mypy overrides updated, signal_types.py defines the full signal contract</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/alpha/ scaffold with conftest and all test stubs</name>
  <files>tests/alpha/__init__.py, tests/alpha/conftest.py, tests/alpha/test_regime_detector.py, tests/alpha/test_calibration.py, tests/alpha/test_cointegration.py, tests/alpha/test_carry.py, tests/alpha/test_features.py, tests/alpha/test_walk_forward.py, tests/alpha/test_ensemble.py, tests/alpha/test_orchestrator.py</files>
  <read_first>
    - tests/conftest.py (project-level conftest patterns)
    - tests/backtest/conftest.py (if exists, for fixture style reference)
    - _docs/Phase_3_Alpha_Engines.md (validation criteria per task)
    - .planning/phases/03-alpha-engines/03-RESEARCH.md (test map section)
    - src/alpha/signal_types.py (just created in Task 1)
  </read_first>
  <action>
    1. Create `tests/alpha/__init__.py` (empty)
    2. Create `tests/alpha/conftest.py` with shared fixtures:
       - `synthetic_returns(n=2000)`: np.ndarray of regime-switching returns — use 3 segments of 666 bars each with different vol (0.005, 0.01, 0.03) to simulate trending/mean-reverting/crisis
       - `cointegrated_pair(n=1000)`: tuple of (y1, y2) arrays where y2 = 0.8*y1 + noise (known hedge ratio ~0.8)
       - `sample_bar_data(n=500)`: dict with keys 'open', 'high', 'low', 'close', 'tick_volume' as np.ndarrays
       - `sample_signal_df()`: pd.DataFrame with 10 rows matching SignalRow schema
       - Import `RegimeState` from `src.alpha.signal_types`
    3. Create test stubs for each test file. Each stub must:
       - Import pytest
       - Have 2-4 test functions with descriptive names matching the validation criteria from the spec
       - Mark each test with `@pytest.mark.skip(reason="Stub — implementation in plan 03-0X")`
       - Include a brief docstring describing what the test will verify

       Test files and their stub functions:

       **test_regime_detector.py** (ALPH-01, ALPH-02):
       - `test_hmm_garch_fits_three_states` — verifies 3 distinct states on synthetic data
       - `test_garch_stationarity_constraint` — alpha + beta < 1 for all states
       - `test_states_sorted_by_ascending_variance` — state 0 has lowest unconditional variance
       - `test_online_prediction_matches_viterbi` — >90% agreement

       **test_calibration.py** (ALPH-03):
       - `test_weekly_recalibration_produces_valid_model` — fit on fresh data
       - `test_dirichlet_smoothing_no_zero_transitions` — no transition prob is exactly 0
       - `test_parameter_drift_warning` — >50% drift triggers WARNING
       - `test_stationarity_gate_rejects_invalid` — alpha+beta >= 1 rejects
       - `test_state_agreement_gate` — <90% agreement rejects

       **test_cointegration.py** (ALPH-04, ALPH-05):
       - `test_johansen_detects_cointegrated_pair` — rank=1 on synthetic data
       - `test_hedge_ratio_converges` — within 5% of true value
       - `test_zscore_entry_signals` — fires at +/-2.0
       - `test_zscore_hard_stop` — fires at +/-4.0
       - `test_half_life_computation` — matches known AR(1) coefficient

       **test_carry.py** (ALPH-06):
       - `test_forex_carry_annualized_values` — correct carry on known swaps
       - `test_cross_sectional_ranking` — top/bottom quartile correct
       - `test_spread_filter_suppresses` — carry < 2x spread suppressed
       - `test_futures_carry_stub_raises` — NotImplementedError

       **test_features.py** (ALPH-07):
       - `test_all_27_features_finite` — no inf/nan after warmup
       - `test_pit_compliance_shift` — features at T from data at T-1
       - `test_feature_computation_performance` — 1M bars < 5s
       - `test_no_high_correlation_pairs` — |corr| < 0.95

       **test_walk_forward.py** (ALPH-08):
       - `test_walk_forward_produces_30_windows` — 5 years data
       - `test_no_data_leakage_purge` — test data not in train
       - `test_cost_adjusted_sharpe` — gross > net

       **test_ensemble.py** (ALPH-08):
       - `test_ensemble_probability_bounded` — [0, 1]
       - `test_xgboost_callbacks_in_constructor` — no TypeError
       - `test_shap_values_sum_to_output` — SHAP identity

       **test_orchestrator.py** (ALPH-09):
       - `test_trending_activates_ml_and_carry` — per D-05
       - `test_mean_reverting_activates_cointegration` — per D-05
       - `test_crisis_activates_nothing` — empty list
       - `test_hysteresis_20_bars` — no state change within dwell
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix && .venv/bin/python -m pytest tests/alpha/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/alpha/__init__.py` exists
    - `tests/alpha/conftest.py` contains `synthetic_returns`, `cointegrated_pair`, `sample_bar_data` fixtures
    - `pytest --collect-only tests/alpha/` discovers at least 30 test functions
    - All discovered tests show `skip` status when run (not errors)
    - Each test file has at least 2 test functions
    - `grep -r "pytest.mark.skip" tests/alpha/ | wc -l` returns >= 30
  </acceptance_criteria>
  <done>All 9 test files exist with skip-marked stubs, conftest provides shared fixtures, pytest discovers all tests</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -c "import shap; print(shap.__version__)"` prints `0.51.0`
- `grep "shap" pyproject.toml` shows shap in mypy overrides
- `.venv/bin/python -c "from src.alpha.signal_types import SignalRow, RegimeState"` succeeds
- `pytest tests/alpha/ --collect-only -q` discovers 30+ tests
- `pytest tests/alpha/ -x -q --no-cov` passes (all skipped)
</verification>

<success_criteria>
- shap 0.51.0 installed and importable
- Signal schema contract defined and importable
- All 9 test files exist with meaningful stubs
- pytest discovers and collects all test stubs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-alpha-engines/03-01-SUMMARY.md`
</output>

---
phase: 03-alpha-engines
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/alpha/regime/hmm_garch.py
  - src/alpha/regime/emissions.py
  - src/alpha/regime/online_filter.py
  - src/alpha/regime/viterbi.py
  - src/alpha/regime/__init__.py
  - tests/alpha/test_regime_detector.py
autonomous: true
requirements:
  - ALPH-01
  - ALPH-02

must_haves:
  truths:
    - "HMMGARCHRegimeDetector.fit() classifies returns into 3 distinct states"
    - "GARCH stationarity alpha+beta<1 is enforced for all states"
    - "States are sorted by ascending unconditional variance after every fit"
    - "Online forward filter matches offline Viterbi on >90% of data points"
  artifacts:
    - path: "src/alpha/regime/hmm_garch.py"
      provides: "HMMGARCHRegimeDetector class with fit() and predict() methods"
      exports: ["HMMGARCHRegimeDetector"]
      min_lines: 100
    - path: "src/alpha/regime/emissions.py"
      provides: "GARCH emission PDF computation"
      exports: ["garch_emission_prob", "GARCHParams"]
      min_lines: 40
    - path: "src/alpha/regime/online_filter.py"
      provides: "Forward-only real-time regime filter"
      exports: ["OnlineRegimeFilter"]
      min_lines: 50
    - path: "src/alpha/regime/viterbi.py"
      provides: "Offline log-space Viterbi decoding"
      exports: ["viterbi_decode"]
      min_lines: 30
  key_links:
    - from: "src/alpha/regime/hmm_garch.py"
      to: "src/alpha/regime/emissions.py"
      via: "import GARCHParams for per-state parameters"
      pattern: "from src.alpha.regime.emissions import"
    - from: "src/alpha/regime/hmm_garch.py"
      to: "hmmlearn.hmm.GaussianHMM"
      via: "Stage 1 base HMM fit"
      pattern: "GaussianHMM"
    - from: "src/alpha/regime/online_filter.py"
      to: "src/alpha/regime/emissions.py"
      via: "garch_emission_prob for online prediction"
      pattern: "garch_emission_prob"
---

<objective>
Implement the HMM-GARCH regime detector core: two-stage fitting (GaussianHMM + per-state GARCH), GARCH emission computation, forward-only online filter, and offline Viterbi decoder.

Purpose: The regime detector is the master switch for all strategy activation. Every other engine depends on knowing the current regime state. This must be complete and tested before cointegration, carry, or ML engines can be gated.
Output: Working HMMGARCHRegimeDetector that fits, predicts online, and decodes offline with deterministic state ordering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-alpha-engines/03-CONTEXT.md
@.planning/phases/03-alpha-engines/03-RESEARCH.md
@_docs/Phase_3_Alpha_Engines.md

@.planning/phases/03-alpha-engines/03-01-SUMMARY.md

<interfaces>
From src/alpha/signal_types.py (created in plan 01):
```python
class RegimeState(enum.IntEnum):
    TRENDING = 0
    MEAN_REVERTING = 1
    CRISIS = 2
```

From hmmlearn 0.3.3:
```python
model = GaussianHMM(n_components=3, covariance_type='diag', n_iter=100, tol=0.01, random_state=seed)
model.fit(returns.reshape(-1, 1))
model.monitor_.converged  # bool — check AFTER fit()
states = model.predict(returns.reshape(-1, 1))
model.transmat_  # (3, 3) transition matrix
```

From arch 8.0.0:
```python
res = arch_model(state_returns, vol='Garch', p=1, q=1, dist='normal').fit(disp='off')
res.params['mu'], res.params['omega'], res.params['alpha[1]'], res.params['beta[1]']
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GARCH emissions and Viterbi decoder</name>
  <files>src/alpha/regime/emissions.py, src/alpha/regime/viterbi.py</files>
  <read_first>
    - _docs/Phase_3_Alpha_Engines.md (GARCH emission formula, Viterbi section)
    - .planning/phases/03-alpha-engines/03-RESEARCH.md (Pattern 1: Two-Stage HMM-GARCH Fit, arch param keys)
    - src/alpha/regime/__init__.py (current stub)
    - src/alpha/signal_types.py (RegimeState enum)
  </read_first>
  <behavior>
    - Test 1: GARCHParams dataclass stores mu, omega, alpha, beta and computes unconditional_variance = omega / (1 - alpha - beta)
    - Test 2: garch_emission_prob computes correct PDF for known sigma^2, returns, mu values (compare to scipy.stats.norm)
    - Test 3: GARCH variance recursion sigma^2_t = omega + alpha*eps^2_{t-1} + beta*sigma^2_{t-1} produces finite positive values
    - Test 4: viterbi_decode on a 3-state toy example with known transition matrix returns the correct optimal path
    - Test 5: viterbi_decode output length equals input length
  </behavior>
  <action>
    1. Create `src/alpha/regime/emissions.py`:
       - `@dataclass(frozen=True)` class `GARCHParams` with fields: `mu: float`, `omega: float`, `alpha: float`, `beta: float`
       - Property `unconditional_variance` returning `self.omega / (1 - self.alpha - self.beta)`
       - Property `is_stationary` returning `self.alpha + self.beta < 1.0`
       - Function `garch_emission_prob(returns: np.ndarray, params: GARCHParams) -> np.ndarray`:
         - Computes GARCH(1,1) conditional variance recursion: `sigma2[t] = omega + alpha * eps2[t-1] + beta * sigma2[t-1]`
         - Initialize `sigma2[0] = params.unconditional_variance`
         - `eps2[t] = (returns[t] - mu)^2`
         - Returns emission log-probabilities: `log_b[t] = -0.5 * (log(2*pi) + log(sigma2[t]) + eps2[t] / sigma2[t])`

    2. Create `src/alpha/regime/viterbi.py`:
       - Function `viterbi_decode(log_emission_probs: np.ndarray, log_transmat: np.ndarray, log_startprob: np.ndarray) -> np.ndarray`:
         - `log_emission_probs` shape: `(T, n_states)`
         - `log_transmat` shape: `(n_states, n_states)`
         - `log_startprob` shape: `(n_states,)`
         - Log-space computation throughout (no underflow)
         - Standard Viterbi: delta, psi matrices, backtracking
         - Returns `states: np.ndarray` of shape `(T,)` with integer state labels

    3. Update `src/alpha/regime/__init__.py` to export `GARCHParams`, `garch_emission_prob`, `viterbi_decode`
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix && .venv/bin/python -m pytest tests/alpha/test_regime_detector.py -x -q --no-cov -k "emission or viterbi or garch_params" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class GARCHParams" src/alpha/regime/emissions.py` finds the dataclass
    - `grep "unconditional_variance" src/alpha/regime/emissions.py` finds the property
    - `grep "def garch_emission_prob" src/alpha/regime/emissions.py` finds the function
    - `grep "def viterbi_decode" src/alpha/regime/viterbi.py` finds the function
    - `grep "log_transmat" src/alpha/regime/viterbi.py` confirms log-space computation
    - All emission and viterbi tests pass
  </acceptance_criteria>
  <done>GARCH emission PDF computation works correctly, Viterbi produces optimal path in log-space</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement HMMGARCHRegimeDetector and OnlineRegimeFilter</name>
  <files>src/alpha/regime/hmm_garch.py, src/alpha/regime/online_filter.py, src/alpha/regime/__init__.py, tests/alpha/test_regime_detector.py</files>
  <read_first>
    - _docs/Phase_3_Alpha_Engines.md (Task 3A.1 — full HMM-GARCH spec)
    - .planning/phases/03-alpha-engines/03-RESEARCH.md (Pattern 1, hmmlearn API, Pitfall 2 state instability, Pitfall 5 small subsets)
    - src/alpha/regime/emissions.py (just created — GARCHParams, garch_emission_prob)
    - src/alpha/regime/viterbi.py (just created — viterbi_decode)
    - src/alpha/signal_types.py (RegimeState enum)
    - tests/alpha/test_regime_detector.py (test stubs to unstub)
    - tests/alpha/conftest.py (synthetic_returns fixture)
  </read_first>
  <behavior>
    - Test 1: fit() on synthetic 3-regime data produces model with 3 states where each state has valid GARCHParams
    - Test 2: After fit(), states are sorted by ascending unconditional_variance (state 0 = lowest vol = TRENDING)
    - Test 3: fit() rejects model where alpha+beta >= 1 for any state (returns False or raises)
    - Test 4: fit_with_retry tries up to 5 seeds when EM fails to converge
    - Test 5: OnlineRegimeFilter.update(return_value) returns RegimeState and confidence
    - Test 6: Online predictions match Viterbi on >90% of synthetic data points
    - Test 7: States with < 100 samples fall back to Gaussian emission (not GARCH)
  </behavior>
  <action>
    1. Create `src/alpha/regime/hmm_garch.py`:
       - Class `HMMGARCHRegimeDetector`:
         - `__init__(self, n_states: int = 3, n_iter: int = 100, tol: float = 0.01, max_retries: int = 5, min_state_samples: int = 100)`
         - `fit(self, returns: np.ndarray) -> bool`:
           - Stage 1: `GaussianHMM(n_components=n_states, covariance_type='diag', n_iter=n_iter, tol=tol, random_state=seed)` with retry loop (up to max_retries seeds)
           - Check `model.monitor_.converged` after fit (NOT `model.monitor.converged` -- note the underscore)
           - `states = model.predict(returns.reshape(-1, 1))`
           - Stage 2: For each state j, extract `returns[states == j]`; skip GARCH if len < min_state_samples (use Gaussian fallback)
           - `arch_model(state_returns, vol='Garch', p=1, q=1, dist='normal').fit(disp='off')`
           - Extract params: `res.params['mu']`, `res.params['omega']`, `res.params['alpha[1]']`, `res.params['beta[1]']`
           - Stationarity: if `alpha + beta >= 1` for ANY state, reject entire fit, return False
           - Sort states by ascending `unconditional_variance` = `omega/(1-alpha-beta)` — re-map all state labels
           - Store: `self.garch_params: list[GARCHParams]` (length 3, sorted), `self.transmat_: np.ndarray` (3x3, re-sorted), `self.startprob_: np.ndarray` (3, re-sorted), `self._fitted: bool = True`
           - Return True on success
         - `predict_viterbi(self, returns: np.ndarray) -> np.ndarray`: compute GARCH emission log-probs per state, call viterbi_decode
         - `get_regime_label(self, state: int) -> str`: maps 0->"TRENDING", 1->"MEAN_REVERTING", 2->"CRISIS"
         - Property `is_fitted -> bool`
         - Logging to `logging.getLogger("helix.alpha")`

    2. Create `src/alpha/regime/online_filter.py`:
       - Class `OnlineRegimeFilter`:
         - `__init__(self, detector: HMMGARCHRegimeDetector)`: stores detector reference, initializes `self._alpha: np.ndarray` (forward variable, shape n_states), `self._sigma2: np.ndarray` (per-state conditional variance, init to unconditional)
         - `update(self, return_value: float) -> tuple[RegimeState, float]`:
           - Forward algorithm step: for each state, compute emission prob using GARCH recursion, multiply by transition, normalize
           - Update `self._sigma2` per state: `sigma2[j] = omega_j + alpha_j * eps2 + beta_j * sigma2[j]` where `eps = return_value - mu_j`
           - Return `(RegimeState(argmax(alpha)), max(alpha))` — regime and confidence
         - `reset(self)`: re-initialize alpha to startprob, sigma2 to unconditional
         - Property `state_probs -> np.ndarray`: returns current normalized forward variable

    3. Update `src/alpha/regime/__init__.py` to export `HMMGARCHRegimeDetector`, `OnlineRegimeFilter`

    4. Unstub the test functions in `tests/alpha/test_regime_detector.py`: remove `@pytest.mark.skip` decorators, write real test implementations using the `synthetic_returns` fixture from conftest.py. The tests must:
       - `test_hmm_garch_fits_three_states`: call `detector.fit(synthetic_returns)`, assert `detector.is_fitted`, assert `len(detector.garch_params) == 3`
       - `test_garch_stationarity_constraint`: assert `all(p.is_stationary for p in detector.garch_params)`
       - `test_states_sorted_by_ascending_variance`: assert `detector.garch_params[0].unconditional_variance < detector.garch_params[1].unconditional_variance < detector.garch_params[2].unconditional_variance`
       - `test_online_prediction_matches_viterbi`: run both, compute agreement ratio, assert > 0.90
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix && .venv/bin/python -m pytest tests/alpha/test_regime_detector.py -x -q --no-cov 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class HMMGARCHRegimeDetector" src/alpha/regime/hmm_garch.py` finds the class
    - `grep "monitor_.converged" src/alpha/regime/hmm_garch.py` uses underscore (not dot)
    - `grep "alpha\[1\]" src/alpha/regime/hmm_garch.py` uses exact arch param key
    - `grep "beta\[1\]" src/alpha/regime/hmm_garch.py` uses exact arch param key
    - `grep "class OnlineRegimeFilter" src/alpha/regime/online_filter.py` finds the class
    - `grep "RegimeState" src/alpha/regime/online_filter.py` confirms return type
    - All 4 test functions in test_regime_detector.py pass (no skip markers remain)
    - `pytest tests/alpha/test_regime_detector.py` exits with 0
  </acceptance_criteria>
  <done>HMM-GARCH detector fits 3 states with stationarity enforcement, online filter tracks regime in real-time, all tests pass</done>
</task>

</tasks>

<verification>
- `pytest tests/alpha/test_regime_detector.py -x -q --no-cov` — all tests pass
- `from src.alpha.regime import HMMGARCHRegimeDetector, OnlineRegimeFilter, GARCHParams, viterbi_decode` — all imports work
- State ordering is deterministic: state 0 always has lowest unconditional variance
</verification>

<success_criteria>
- HMMGARCHRegimeDetector.fit() produces 3 states sorted by ascending variance on synthetic data
- GARCH stationarity (alpha+beta<1) enforced for all states
- OnlineRegimeFilter tracks regime state with forward-only algorithm
- Viterbi decoder produces optimal state sequence in log-space
- All test_regime_detector.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-alpha-engines/03-02-SUMMARY.md`
</output>

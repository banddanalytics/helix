---
phase: 03-alpha-engines
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/alpha/cointegration/johansen.py
  - src/alpha/cointegration/hedge_ratio.py
  - src/alpha/cointegration/spread_signals.py
  - src/alpha/cointegration/health_monitor.py
  - src/alpha/cointegration/__init__.py
  - tests/alpha/test_cointegration.py
autonomous: true
requirements:
  - ALPH-04
  - ALPH-05

must_haves:
  truths:
    - "Johansen trace test correctly identifies rank=1 cointegration on known pairs"
    - "504-bar rolling hedge ratio converges to true value within 5%"
    - "Z-score entry signals fire at +/-2.0, exit at +/-0.5, hard stop at +/-4.0"
    - "Half-life computation matches known AR(1) coefficient"
    - "All rolling computations are PiT compliant (no future data)"
  artifacts:
    - path: "src/alpha/cointegration/johansen.py"
      provides: "Johansen trace test with rank detection"
      exports: ["test_cointegration", "JohansenResult"]
      min_lines: 30
    - path: "src/alpha/cointegration/hedge_ratio.py"
      provides: "Dynamic 504-bar rolling hedge ratio"
      exports: ["RollingHedgeRatio"]
      min_lines: 40
    - path: "src/alpha/cointegration/spread_signals.py"
      provides: "Z-score entry/exit/hard-stop signal generator"
      exports: ["SpreadSignalGenerator"]
      min_lines: 60
    - path: "src/alpha/cointegration/health_monitor.py"
      provides: "Half-life AR(1) and cointegration breakdown detection"
      exports: ["CointegrationHealthMonitor"]
      min_lines: 40
  key_links:
    - from: "src/alpha/cointegration/spread_signals.py"
      to: "src/alpha/cointegration/hedge_ratio.py"
      via: "RollingHedgeRatio provides beta for spread computation"
      pattern: "RollingHedgeRatio"
    - from: "src/alpha/cointegration/johansen.py"
      to: "statsmodels.tsa.vector_ar.vecm.coint_johansen"
      via: "Johansen trace test"
      pattern: "coint_johansen"
---

<objective>
Implement the Johansen cointegration engine: trace test for pair detection, 504-bar rolling hedge ratio, z-score entry/exit/hard-stop signal generation, and half-life health monitoring.

Purpose: The cointegration engine is the primary Mean-Reverting regime strategy. It detects statistically cointegrated Forex pairs and generates trading signals when the spread deviates from equilibrium. This runs only when the regime detector reports MEAN_REVERTING state.
Output: Complete cointegration pipeline from pair testing to signal generation with PiT compliance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-alpha-engines/03-CONTEXT.md
@.planning/phases/03-alpha-engines/03-RESEARCH.md
@_docs/Phase_3_Alpha_Engines.md

@.planning/phases/03-alpha-engines/03-01-SUMMARY.md

<interfaces>
From src/alpha/signal_types.py (created in plan 01):
```python
class SignalRow:
    symbol: str; engine: str; direction: int; strength: float; regime: int
    z_score: float | None = None; ml_prob: float | None = None; carry_rank: float | None = None

CONFIGURED_PAIRS = [("AUDUSD", "NZDUSD"), ("EURUSD", "GBPUSD"), ("USDJPY", "USDCHF")]
```

From statsmodels 0.14.6 (verified in research):
```python
from statsmodels.tsa.vector_ar.vecm import coint_johansen
result = coint_johansen(data, det_order=0, k_ar_diff=1)
result.trace_stat[0]                # scalar for H0: rank=0
result.trace_stat_crit_vals[0, 1]   # 95% critical value
hedge_ratio = -result.evec[1, 0] / result.evec[0, 0]  # VERIFIED indexing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Johansen trace test and rolling hedge ratio</name>
  <files>src/alpha/cointegration/johansen.py, src/alpha/cointegration/hedge_ratio.py, src/alpha/cointegration/__init__.py</files>
  <read_first>
    - _docs/Phase_3_Alpha_Engines.md (Task 3B.1 — Johansen spec, hedge ratio window)
    - .planning/phases/03-alpha-engines/03-RESEARCH.md (Pattern 2: Johansen, Pitfall 3: look-ahead in rolling hedge)
    - src/alpha/signal_types.py (CONFIGURED_PAIRS)
    - src/alpha/cointegration/__init__.py (current stub)
  </read_first>
  <behavior>
    - Test 1: test_cointegration() detects rank=1 on synthetic cointegrated pair (trace_stat > crit_95)
    - Test 2: test_cointegration() returns cointegrated=False on independent random walks
    - Test 3: Hedge ratio from Johansen converges to true beta within 5% on synthetic data (true beta = 0.8)
    - Test 4: RollingHedgeRatio with window=504 produces PiT-compliant ratios (data[t-504:t], not [t-504:t+1])
  </behavior>
  <action>
    1. Create `src/alpha/cointegration/johansen.py`:
       - `from dataclasses import dataclass`
       - `from statsmodels.tsa.vector_ar.vecm import coint_johansen`
       - `@dataclass(frozen=True)` class `JohansenResult`:
         - `cointegrated: bool`
         - `trace_stat: float`
         - `crit_95: float`
         - `hedge_ratio: float`
       - Function `test_cointegration(y1: np.ndarray, y2: np.ndarray) -> JohansenResult`:
         - `data = np.column_stack([y1, y2])`
         - `result = coint_johansen(data, det_order=0, k_ar_diff=1)`
         - `trace_stat = result.trace_stat[0]`
         - `crit_95 = result.trace_stat_crit_vals[0, 1]` (col 1 = 95%)
         - `hedge_ratio = -result.evec[1, 0] / result.evec[0, 0]` (CRITICAL: negative sign, [1,0]/[0,0])
         - Return `JohansenResult(cointegrated=trace_stat > crit_95, ...)`

    2. Create `src/alpha/cointegration/hedge_ratio.py`:
       - Class `RollingHedgeRatio`:
         - `__init__(self, window: int = 504, step: int = 21)`: stores window/step config
         - `compute(self, y1: np.ndarray, y2: np.ndarray) -> np.ndarray`:
           - Returns array of hedge ratios, one per bar
           - For t in range(window, len(y1)):
             - Use data `y1[t-window:t]`, `y2[t-window:t]` (Python slice excludes t — PiT correct)
             - Only recompute every `step` bars; carry forward previous value otherwise
             - Call `test_cointegration(y1_slice, y2_slice).hedge_ratio`
           - First `window` bars: NaN (insufficient data)
           - Returns `np.ndarray` of shape `(len(y1),)`

    3. Update `src/alpha/cointegration/__init__.py` to export `test_cointegration`, `JohansenResult`, `RollingHedgeRatio`
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix && .venv/bin/python -m pytest tests/alpha/test_cointegration.py -x -q --no-cov -k "johansen or hedge" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "coint_johansen" src/alpha/cointegration/johansen.py` finds the import
    - `grep "evec\[1, 0\]" src/alpha/cointegration/johansen.py` uses correct indexing
    - `grep "evec\[0, 0\]" src/alpha/cointegration/johansen.py` uses correct denominator
    - `grep "t-window:t\]" src/alpha/cointegration/hedge_ratio.py` confirms PiT slicing (exclusive end)
    - Johansen and hedge ratio tests pass
  </acceptance_criteria>
  <done>Johansen test detects cointegrated pairs, rolling hedge ratio is PiT compliant</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement spread z-score signals and health monitor</name>
  <files>src/alpha/cointegration/spread_signals.py, src/alpha/cointegration/health_monitor.py, src/alpha/cointegration/__init__.py, tests/alpha/test_cointegration.py</files>
  <read_first>
    - _docs/Phase_3_Alpha_Engines.md (z-score thresholds, half-life formula, hard stop)
    - .planning/phases/03-alpha-engines/03-CONTEXT.md (D-01 signal schema)
    - src/alpha/cointegration/johansen.py (just created)
    - src/alpha/cointegration/hedge_ratio.py (just created)
    - src/alpha/signal_types.py (SignalRow)
    - tests/alpha/test_cointegration.py (stubs to unstub)
    - tests/alpha/conftest.py (cointegrated_pair fixture)
  </read_first>
  <behavior>
    - Test 1: Z-score entry long fires when Z < -2.0 (direction = +1)
    - Test 2: Z-score entry short fires when Z > +2.0 (direction = -1)
    - Test 3: Z-score exit long fires when Z > -0.5 (direction = 0)
    - Test 4: Z-score exit short fires when Z < +0.5 (direction = 0)
    - Test 5: Hard stop fires when |Z| > 4.0 (direction = 0, strength = 1.0 for urgency)
    - Test 6: Half-life matches expected value for known AR(1) process (delta = 0.95 -> HL = -ln(2)/ln(0.95) ~ 13.5)
    - Test 7: HL > 60 triggers position reduction flag; HL > 120 triggers close-all flag
  </behavior>
  <action>
    1. Create `src/alpha/cointegration/spread_signals.py`:
       - Class `SpreadSignalGenerator`:
         - `__init__(self, entry_z: float = 2.0, exit_z: float = 0.5, hard_stop_z: float = 4.0, lookback: int = 252)`:
         - `compute_spread(self, y1: np.ndarray, y2: np.ndarray, hedge_ratio: np.ndarray) -> np.ndarray`:
           - `spread = y1 - hedge_ratio * y2`
         - `compute_zscore(self, spread: np.ndarray) -> np.ndarray`:
           - Rolling mean and std on `.shift(1)` equivalent: `spread[i-lookback:i]` (PiT)
           - `z = (spread[i] - mean) / std`
         - `generate_signals(self, z_scores: np.ndarray) -> list[tuple[int, float]]`:
           - Returns list of (direction, strength) tuples per bar
           - Entry long: `z < -entry_z` -> `(+1, min(abs(z)/entry_z, 1.0))`
           - Entry short: `z > +entry_z` -> `(-1, min(abs(z)/entry_z, 1.0))`
           - Exit long: `z > -exit_z` (was long) -> `(0, 0.0)`
           - Exit short: `z < +exit_z` (was short) -> `(0, 0.0)`
           - Hard stop: `abs(z) > hard_stop_z` -> `(0, 1.0)` with is_hard_stop=True
           - Flat: `(0, 0.0)`

    2. Create `src/alpha/cointegration/health_monitor.py`:
       - Class `CointegrationHealthMonitor`:
         - `__init__(self, hl_reduce_threshold: int = 60, hl_close_threshold: int = 120)`:
         - `compute_half_life(self, spread: np.ndarray) -> float`:
           - AR(1) regression: `delta = OLS(spread[1:] ~ spread[:-1]).coef`
           - `half_life = -np.log(2) / np.log(abs(delta))` (use abs to handle negative delta)
           - Return half_life in bars
         - `assess_health(self, spread: np.ndarray, trace_stat: float, crit_10: float) -> dict`:
           - Returns dict with keys: `half_life: float`, `reduce_position: bool` (HL > 60), `close_all: bool` (HL > 120), `suspend: bool` (trace_stat < crit_10)
         - `check_breakdown(self, trace_stat: float, crit_10: float) -> bool`:
           - Returns True if trace_stat < crit_10 (cointegration breaking down)

    3. Update `src/alpha/cointegration/__init__.py` to export `SpreadSignalGenerator`, `CointegrationHealthMonitor`

    4. Unstub all test functions in `tests/alpha/test_cointegration.py`: remove `@pytest.mark.skip`, implement real tests using `cointegrated_pair` fixture:
       - `test_johansen_detects_cointegrated_pair`: use fixture, assert `result.cointegrated is True`
       - `test_hedge_ratio_converges`: assert `abs(result.hedge_ratio - 0.8) < 0.05` (fixture uses beta=0.8)
       - `test_zscore_entry_signals`: create spread with known z > 2.0, assert direction == -1
       - `test_zscore_hard_stop`: create spread with z > 4.0, assert strength == 1.0
       - `test_half_life_computation`: create AR(1) with delta=0.95, assert `abs(hl - 13.5) < 1.0`
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix && .venv/bin/python -m pytest tests/alpha/test_cointegration.py -x -q --no-cov 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "entry_z.*2.0" src/alpha/cointegration/spread_signals.py` finds entry threshold
    - `grep "hard_stop_z.*4.0" src/alpha/cointegration/spread_signals.py` finds hard stop
    - `grep "np.log(2)" src/alpha/cointegration/health_monitor.py` finds half-life formula
    - `grep -c "pytest.mark.skip" tests/alpha/test_cointegration.py` returns 0
    - All test_cointegration.py tests pass
  </acceptance_criteria>
  <done>Z-score signals fire at correct thresholds, half-life correctly computed, all cointegration tests pass</done>
</task>

</tasks>

<verification>
- `pytest tests/alpha/test_cointegration.py -x -q --no-cov` — all tests pass
- `from src.alpha.cointegration import test_cointegration, SpreadSignalGenerator, CointegrationHealthMonitor` — imports work
- Z-score entry at +/-2.0, hard stop at +/-4.0 verified by tests
</verification>

<success_criteria>
- Johansen trace test identifies cointegrated pairs (rank=1 detection)
- 504-bar rolling hedge ratio converges to true value within 5%
- Z-score entry at +/-2.0, exit at +/-0.5, hard stop at +/-4.0
- Half-life computation matches known AR(1) coefficient
- All rolling computations PiT compliant
- All test_cointegration.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-alpha-engines/03-04-SUMMARY.md`
</output>

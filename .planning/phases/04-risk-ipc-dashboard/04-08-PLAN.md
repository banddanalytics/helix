---
phase: 04-risk-ipc-dashboard
plan: 08
type: execute
wave: 4
depends_on:
  - 04-06
files_modified:
  - dashboard/.env.example
  - dashboard/app/layout.tsx
autonomous: false
requirements:
  - IPC-04
  - IPC-05

must_haves:
  truths:
    - "Dashboard builds successfully for static export"
    - "All 7 routes render without errors"
    - "Error in one route does not crash sibling routes"
    - "Dashboard .env.example documents NEXT_PUBLIC_NATS_WS_URL"
  artifacts:
    - path: "dashboard/.env.example"
      provides: "Environment variable documentation"
      contains: "NEXT_PUBLIC_NATS_WS_URL"
  key_links:
    - from: "dashboard/app/layout.tsx"
      to: "dashboard/components/providers/trading-stage-provider.tsx"
      via: "wraps all routes"
      pattern: "TradingStageProvider"
---

<objective>
Final dashboard verification — ensure the complete Next.js app builds, all routes render, and error boundaries provide isolation.

Purpose: This is the checkpoint verifying that the dashboard is ready for deployment. It creates the .env.example for deployment configuration and verifies the full build pipeline.
Output: Verified dashboard build, .env.example for deployment.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-risk-ipc-dashboard/04-CONTEXT.md
@.planning/phases/04-risk-ipc-dashboard/04-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example and verify full dashboard build</name>
  <files>dashboard/.env.example, dashboard/app/layout.tsx</files>
  <read_first>
    - dashboard/app/layout.tsx (verify TradingStageProvider, nav links, dark mode)
    - dashboard/next.config.ts (verify output: "export" for static deployment)
    - dashboard/components/hooks/use-nats-subscription.ts (verify NEXT_PUBLIC_NATS_WS_URL usage)
    - .planning/phases/04-risk-ipc-dashboard/04-CONTEXT.md (D-06: Vercel deployment)
  </read_first>
  <action>
    1. Create `dashboard/.env.example`:
       ```
       # Helix Dashboard Environment Variables
       # Copy to .env.local for development, set in Vercel for production

       # NATS WebSocket URL — the trading server's NATS WebSocket bridge endpoint
       # Development: ws://localhost:9222
       # Production: wss://your-server:9222 (TLS at reverse proxy)
       NEXT_PUBLIC_NATS_WS_URL=ws://localhost:9222
       ```

    2. Verify `dashboard/app/layout.tsx` has all required elements:
       - TradingStageProvider wrapping children
       - Navigation with links to all 7 routes (/, /regime, /cointegration, /momentum, /carry, /risk, /orders)
       - `className="dark"` on html element
       - Geist fonts imported

    3. Run full build: `cd dashboard && npm run build`
       - Verify build completes without errors
       - Verify all 7 routes are generated in the build output

    4. Add `dashboard/.env.local` to `.gitignore` if not already present (do NOT commit actual env values).
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix/dashboard && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - dashboard/.env.example contains `NEXT_PUBLIC_NATS_WS_URL=ws://localhost:9222`
    - dashboard/.env.example contains `# Production: wss://`
    - npm run build exits 0 in dashboard/
    - Build output includes all 7 routes (/, /regime, /cointegration, /momentum, /carry, /risk, /orders)
  </acceptance_criteria>
  <done>Dashboard builds successfully with all routes, .env.example documents deployment config</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard routes and dark theme</name>
  <files>dashboard/app/layout.tsx</files>
  <action>
    Start the dashboard dev server and verify all routes render correctly with the dark trading terminal aesthetic. This is a human visual verification — Claude cannot evaluate visual appearance.
  </action>
  <verify>
    <automated>cd /home/user/Desktop/Projects/BANDD/helix/dashboard && npm run build 2>&1 | grep -c "Generating static pages" || echo "Build check"</automated>
  </verify>
  <what-built>Complete Phase 4 dashboard: 7-route Next.js app with dark trading terminal theme, shadcn/ui components, useNatsSubscription hook with 100ms batching, and all route pages displaying telemetry data views</what-built>
  <how-to-verify>
    1. Start the dashboard dev server: `cd dashboard && npm run dev`
    2. Open http://localhost:3000 in browser
    3. Verify dark terminal aesthetic (zinc-950 background, zinc-900 cards)
    4. Click through all 7 nav links — each route should render without crashing
    5. Verify each route shows empty state: "No data yet" / "Waiting for telemetry..."
    6. Verify the overview page (/) has the equity curve chart area
    7. Verify the risk page (/risk) has CVaR cards and circuit breaker alert area
    8. Verify the orders page (/orders) has a table structure with pagination
    9. Check that navigation highlights the active route
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms dark theme renders correctly
    - Human confirms all 7 routes are accessible via navigation
    - Human confirms empty states display proper copy text
  </acceptance_criteria>
  <done>Dashboard visually verified: dark theme, 7 routes, proper empty states, no crashes on navigation</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Dashboard build exits 0
- .env.example exists with NATS URL template
- Human verified: all routes render, dark theme correct, empty states display properly
</verification>

<success_criteria>
Dashboard is verified as buildable, deployable to Vercel via static export, and visually correct with the dark trading terminal aesthetic. All 7 routes render with proper error boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/04-risk-ipc-dashboard/04-08-SUMMARY.md`
</output>

---
phase: quick
plan: 260324-8b9
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/get-shit-done/bin/lib/state.cjs
  - ~/.claude/get-shit-done/bin/gsd-tools.cjs
  - ~/.claude/get-shit-done/templates/summary.md
  - ~/.claude/get-shit-done/templates/summary-standard.md
  - ~/.claude/get-shit-done/templates/summary-minimal.md
  - ~/.claude/get-shit-done/templates/summary-complex.md
  - ~/.claude/get-shit-done/workflows/execute-plan.md
  - ~/.claude/get-shit-done/workflows/session-report.md
  - ~/.claude/get-shit-done/templates/state.md
autonomous: true
requirements: [QUICK-260324-8b9]

must_haves:
  truths:
    - "record-metric accepts --cost flag and writes cost column to STATE.md table"
    - "SUMMARY frontmatter includes cost field alongside existing duration field"
    - "execute-plan workflow records cost in summary and passes --cost to record-metric"
    - "session-report aggregates total cost across plans"
  artifacts:
    - path: "~/.claude/get-shit-done/bin/lib/state.cjs"
      provides: "record-metric with cost column support"
      contains: "cost"
    - path: "~/.claude/get-shit-done/templates/summary.md"
      provides: "cost field in frontmatter"
      contains: "cost:"
  key_links:
    - from: "execute-plan.md"
      to: "state.cjs record-metric"
      via: "--cost flag"
      pattern: "--cost"
---

<objective>
Add cost and duration tracking fields to the GSD workflow so that session API costs and elapsed time are captured per plan execution and aggregated in reports.

Purpose: Enable tracking of API spend per plan/phase/session for budget awareness and optimization.
Output: Updated GSD tooling with cost tracking in record-metric, SUMMARY templates, execute-plan workflow, and session-report.
</objective>

<context>
@~/.claude/get-shit-done/bin/lib/state.cjs (cmdStateRecordMetric function, lines 294-326)
@~/.claude/get-shit-done/bin/gsd-tools.cjs (record-metric arg parsing, lines 226-238)
@~/.claude/get-shit-done/templates/summary.md (frontmatter fields)
@~/.claude/get-shit-done/templates/summary-standard.md
@~/.claude/get-shit-done/templates/summary-minimal.md
@~/.claude/get-shit-done/workflows/execute-plan.md (record_start_time, record_completion_time, create_summary, update_state steps)
@~/.claude/get-shit-done/workflows/session-report.md
@~/.claude/get-shit-done/templates/state.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cost field to record-metric CLI and STATE.md table</name>
  <files>
    ~/.claude/get-shit-done/bin/gsd-tools.cjs
    ~/.claude/get-shit-done/bin/lib/state.cjs
    ~/.claude/get-shit-done/templates/state.md
  </files>
  <action>
    1. In `gsd-tools.cjs` (lines 226-238), add `--cost` arg parsing alongside existing `--tasks` and `--files`:
       ```
       const costIdx = args.indexOf('--cost');
       ```
       Pass `cost: costIdx !== -1 ? args[costIdx + 1] : null` to the options object.

    2. In `state.cjs` `cmdStateRecordMetric` function (lines 294-326):
       - Destructure `cost` from options (add to existing `{ phase, plan, duration, tasks, files }`)
       - Update the table row template from:
         `| Phase ${phase} P${plan} | ${duration} | ${tasks || '-'} tasks | ${files || '-'} files |`
         to:
         `| Phase ${phase} P${plan} | ${duration} | ${tasks || '-'} tasks | ${files || '-'} files | ${cost || '-'} |`
       - Include `cost` in the output JSON

    3. In `templates/state.md`, update the Performance Metrics table header to include Cost column:
       ```
       | Phase | Plans | Total | Avg/Plan | Cost |
       |-------|-------|-------|----------|------|
       ```

    4. Update the CLI help comment at top of gsd-tools.cjs (line ~107-109) to document `--cost` option:
       `*   state record-metric --phase N      Record execution metrics`
       `*     --plan M --duration Xmin`
       `*     [--tasks N] [--files N] [--cost $X.XX]`
  </action>
  <verify>
    Run: `node ~/.claude/get-shit-done/bin/gsd-tools.cjs state record-metric --phase test --plan 99 --duration "5 min" --tasks 2 --files 3 --cost "$1.25" --raw` in a temp project with STATE.md — should output JSON with cost field. Then verify the state.md template has Cost column header.
  </verify>
  <done>record-metric CLI accepts --cost, writes cost column to STATE.md metrics table, help text updated</done>
</task>

<task type="auto">
  <name>Task 2: Add cost fields to SUMMARY templates and execute-plan/session-report workflows</name>
  <files>
    ~/.claude/get-shit-done/templates/summary.md
    ~/.claude/get-shit-done/templates/summary-standard.md
    ~/.claude/get-shit-done/templates/summary-minimal.md
    ~/.claude/get-shit-done/templates/summary-complex.md
    ~/.claude/get-shit-done/workflows/execute-plan.md
    ~/.claude/get-shit-done/workflows/session-report.md
  </files>
  <action>
    1. **SUMMARY templates** (all 4 variants — summary.md, summary-standard.md, summary-minimal.md, summary-complex.md):
       - Add `cost: $X.XX` field to frontmatter after existing `duration: Xmin` line
       - In the Performance section body, add `- **API Cost:** $[cost]` after Duration line
       - In summary.md main template, update the frontmatter guidance to mention cost field

    2. **execute-plan.md workflow**:
       - In `<step name="create_summary">` (line ~382), add `cost ($API_COST)` to the frontmatter field list
       - In `<step name="record_completion_time">` (line ~353-368), add a note about capturing API cost:
         ```
         # API cost — if available from session context, record it
         # Format: "$X.XX" (e.g., "$1.25", "$0.50")
         # If not available, use "-"
         API_COST="${API_COST:-"-"}"
         ```
       - In the `record-metric` call (line ~405), append `--cost "${API_COST}"`:
         ```
         node "~/.claude/get-shit-done/bin/gsd-tools.cjs" state record-metric \
           --phase "${PHASE}" --plan "${PLAN}" --duration "${DURATION}" \
           --tasks "${TASK_COUNT}" --files "${FILE_COUNT}" --cost "${API_COST}"
         ```

    3. **session-report.md workflow**:
       - In `<step name="gather_session_data">` (line ~11-39), add instruction to aggregate cost from SUMMARY frontmatter:
         ```
         # Aggregate API costs from summaries written this session
         grep -h "^cost:" .planning/phases/*/*-SUMMARY.md 2>/dev/null | tail -10
         ```
       - In the report template `## Session Summary` section (line ~74-77), add:
         `**Estimated API Cost:** [sum of costs from summaries, or "Not tracked"]`
       - In `## Estimated Resource Usage` table (line ~100-107), add a Cost row:
         `| API Cost | $[total] |`
  </action>
  <verify>
    Grep all modified template files for "cost" to confirm the field is present in frontmatter AND body. Verify execute-plan.md contains `--cost` in the record-metric call. Verify session-report.md mentions API Cost aggregation.
  </verify>
  <done>All SUMMARY templates include cost in frontmatter and Performance section. execute-plan passes cost to record-metric. session-report aggregates costs from summaries.</done>
</task>

</tasks>

<verification>
1. `grep -r "cost" ~/.claude/get-shit-done/bin/lib/state.cjs` shows cost in record-metric function
2. `grep -r "\-\-cost" ~/.claude/get-shit-done/bin/gsd-tools.cjs` shows cost arg parsing
3. `grep -l "^cost:" ~/.claude/get-shit-done/templates/summary*.md` returns all 4 template files
4. `grep "cost" ~/.claude/get-shit-done/workflows/execute-plan.md` shows cost in record-metric call and summary creation
5. `grep "cost\|Cost" ~/.claude/get-shit-done/workflows/session-report.md` shows cost aggregation
</verification>

<success_criteria>
- record-metric CLI accepts optional --cost flag and appends cost to STATE.md metrics table
- All SUMMARY template variants include cost in frontmatter and Performance section
- execute-plan workflow captures API_COST variable and passes to both summary and record-metric
- session-report workflow aggregates cost from SUMMARY frontmatter files
- Existing functionality (duration, tasks, files) is unchanged — cost is additive only
</success_criteria>

<output>
After completion, write summary to `.planning/quick/260324-8b9-add-cost-and-duration-tracking-fields-to/260324-8b9-SUMMARY.md`
</output>

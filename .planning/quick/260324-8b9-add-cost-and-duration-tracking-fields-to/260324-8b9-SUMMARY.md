---
phase: quick
plan: 260324-8b9
subsystem: tooling
tags: [gsd, cost-tracking, metrics, workflow]

provides:
  - cost field in record-metric CLI with --cost flag
  - cost column in STATE.md performance metrics table
  - cost field in all four SUMMARY template variants
  - API_COST variable capture and propagation in execute-plan workflow
  - cost aggregation in session-report workflow
affects: [execute-plan, session-report, state-tracking, summary-templates]

tech-stack:
  added: []
  patterns: [cost tracking in frontmatter, --cost CLI flag pattern]

key-files:
  created: []
  modified:
    - ~/.claude/get-shit-done/bin/gsd-tools.cjs
    - ~/.claude/get-shit-done/bin/lib/state.cjs
    - ~/.claude/get-shit-done/templates/state.md
    - ~/.claude/get-shit-done/templates/summary.md
    - ~/.claude/get-shit-done/templates/summary-standard.md
    - ~/.claude/get-shit-done/templates/summary-minimal.md
    - ~/.claude/get-shit-done/templates/summary-complex.md
    - ~/.claude/get-shit-done/workflows/execute-plan.md
    - ~/.claude/get-shit-done/workflows/session-report.md

key-decisions:
  - "cost field defaults to '-' (not null/empty) for consistent table formatting in STATE.md"
  - "API_COST variable uses bash default syntax API_COST=${API_COST:-'-'} — no external tooling required"
  - "session-report aggregates cost via grep on ^cost: frontmatter lines — simple, no parsing library needed"

requirements-completed: [QUICK-260324-8b9]

duration: 8 min
cost: -
completed: 2026-03-24
---

# Quick Task 260324-8b9: Add Cost and Duration Tracking Fields Summary

**Cost and duration tracking added to GSD tooling: --cost flag for record-metric CLI, cost column in STATE.md metrics table, cost field in all SUMMARY variants, and API_COST propagation through execute-plan and session-report workflows**

## Performance

- **Duration:** 8 min
- **API Cost:** -
- **Started:** 2026-03-24T03:02:20Z
- **Completed:** 2026-03-24T03:10:59Z
- **Tasks:** 2
- **Files modified:** 9 (in ~/.claude/get-shit-done/)

## Accomplishments

- Added `--cost` flag to `gsd-tools.cjs` record-metric arg parser
- Updated `state.cjs` cmdStateRecordMetric to destructure, write, and return `cost` field
- Updated `templates/state.md` Performance Metrics table header to include Cost column
- Updated help text in `gsd-tools.cjs` to document `[--cost $X.XX]` option
- Added `cost: $X.XX` frontmatter field to all four SUMMARY templates (summary.md, summary-standard.md, summary-minimal.md, summary-complex.md)
- Added `- **API Cost:** $[cost]` line to Performance section in all four SUMMARY templates
- Added frontmatter guidance note in summary.md explaining cost field semantics
- Added `API_COST` variable capture with bash default in execute-plan.md `record_completion_time` step
- Updated execute-plan.md `create_summary` frontmatter guidance to include `cost ($API_COST)`
- Updated execute-plan.md `record-metric` call to pass `--cost "${API_COST}"`
- Added cost grep aggregation command to session-report.md `gather_session_data` step
- Added `Estimated API Cost` line to session-report `Session Summary` section
- Added `API Cost` row to session-report `Estimated Resource Usage` table

## Task Commits

Both tasks are in a single planning artifact commit (modified files are in ~/.claude/get-shit-done/ which is not a git repo):

1. **Task 1: Add cost field to record-metric CLI and STATE.md table** - tracked in planning commit
2. **Task 2: Add cost fields to SUMMARY templates and execute-plan/session-report workflows** - tracked in planning commit

## Files Created/Modified

- `~/.claude/get-shit-done/bin/gsd-tools.cjs` - Added costIdx arg parsing, updated help text
- `~/.claude/get-shit-done/bin/lib/state.cjs` - Destructure/write/return cost in cmdStateRecordMetric
- `~/.claude/get-shit-done/templates/state.md` - Added Cost column to Performance Metrics table header
- `~/.claude/get-shit-done/templates/summary.md` - Added cost frontmatter field, API Cost in Performance section, guidance note
- `~/.claude/get-shit-done/templates/summary-standard.md` - Added cost frontmatter field and API Cost in Performance section
- `~/.claude/get-shit-done/templates/summary-minimal.md` - Added cost frontmatter field and API Cost in Performance section
- `~/.claude/get-shit-done/templates/summary-complex.md` - Added cost frontmatter field and API Cost in Performance section
- `~/.claude/get-shit-done/workflows/execute-plan.md` - Added API_COST variable, updated frontmatter guidance, updated record-metric call
- `~/.claude/get-shit-done/workflows/session-report.md` - Added cost grep, Estimated API Cost in summary, API Cost table row

## Decisions Made

- cost field defaults to '-' (not null/empty) for consistent table formatting — empty cells break markdown tables
- `API_COST` uses bash default syntax `${API_COST:-"-"}` so it works without any external tooling
- session-report aggregates cost via `grep -h "^cost:"` on frontmatter — simple grep, no YAML parser needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Self-Check: PASSED

- ~/.claude/get-shit-done/bin/gsd-tools.cjs: FOUND (confirmed --cost present)
- ~/.claude/get-shit-done/bin/lib/state.cjs: FOUND (confirmed cost destructured and written)
- ~/.claude/get-shit-done/templates/state.md: FOUND (confirmed Cost column header)
- ~/.claude/get-shit-done/templates/summary.md: FOUND (confirmed cost: in frontmatter)
- ~/.claude/get-shit-done/templates/summary-standard.md: FOUND (confirmed cost: in frontmatter)
- ~/.claude/get-shit-done/templates/summary-minimal.md: FOUND (confirmed cost: in frontmatter)
- ~/.claude/get-shit-done/templates/summary-complex.md: FOUND (confirmed cost: in frontmatter)
- ~/.claude/get-shit-done/workflows/execute-plan.md: FOUND (confirmed --cost in record-metric call)
- ~/.claude/get-shit-done/workflows/session-report.md: FOUND (confirmed API Cost aggregation)
